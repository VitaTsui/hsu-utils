import { describe, expect, test } from '@jest/globals'
import { get_string_width } from '../..'
import 'jest-canvas-mock'

describe('get_string_width', () => {
  // TODO: canvas 包安装过于繁琐与困难，暂不进行此项单元测试
  // test(`get_string_width('测试-test-1') = 61.92`, () => {
  //   const width = get_string_width('测试-test-1')
  //   expect(width === 61.92).toBe(true)
  // })
})
